@{
    ViewData["Title"] = "Home Page";
}

@if (User != null && User.IsInRole("admin"))
{
    <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register new aplication user</a>
}